@model DataArt.Test.Models.CardViewModel

@{
    ViewBag.Title = "EnterCardNumber";
}

@using (Html.BeginForm("EnterCardNumber", "Authentication", FormMethod.Post))
{
    @Html.LabelFor(m => m.CardNumber)
    <br/>
    @Html.EditorFor(m => m.CardNumber)
    <input type="submit" value="Confirm" />
}